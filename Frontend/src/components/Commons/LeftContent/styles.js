export const styles = (theme) => ({ 
    grid: {
        padding: 5
    },
    paper: {
        padding: 5,
        marginTop: 5,
        marginBottom: 5,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: theme.palette.secondary.main
    },
    paperFilters: {
        display: 'block',
        padding: 0,
        marginTop: 5,
        marginBottom: 5,
        backgroundColor: theme.palette.secondary.main
    }
});